require("fluoromachine").setup({
    glow = false,
    brightness = 0.05,
    theme = "fluoromachine",
    --theme = "retrowave",
    --theme = "delta",
    transparent = vim.g.transparent_enabled,
})
