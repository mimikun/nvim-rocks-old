require("vfiler/config").setup({
    options = {
        auto_cd = true,
        auto_resize = true,
        keep = true,
        layout = "left",
        name = "explorer",
        width = 30,
        columns = "indent,devicons,name",
    },
})
