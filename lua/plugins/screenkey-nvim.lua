require("screenkey").setup({
    disable = {
        -- for example: "toggleterm"
        filetypes = { "toggleterm" },
        -- :h 'buftype'
        -- for example: "terminal"
        buftypes = { "terminal" },
    },
})
